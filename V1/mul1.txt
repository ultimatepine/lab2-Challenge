// R0 == op2, R1 == op1, R2 == op2_shifted, R3 == sum
MOV R0, #5 // 0, op2 = 5
MOV R1, #12 // 1, op1 = 12
MOV R3, #0 // 2, sum = 0
MOV R2, R0 // 3, op2_shifted = op2
CMP R1, #0 // 4
JEQ 10 // 5, while(op1 != 0)

MOV R4, R1 // 6
AND R4, #1 // 7
CMP R4, #1 // 8
JNE 2 // 9, if(op1 & 1)
ADD R3, R3, R2 // 10, sum = sum + op2_shifted

ADD R2, R2 // 11, op2_shifted = op2_shifted << 1
LSR R1, R1, #1 // 12, op1 = op1 >> 1
JMP -9 // 13, while(op1 != 0)
